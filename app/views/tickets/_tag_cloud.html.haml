%h1= title
- count=4
- TicketTag.tag_cloud.each do |name,num|
  = link_to("#{name}(#{num})", {controller: "tickets", action: "search", tag: name, method: "or", target: "ticket"}, remote: true, class: "tag#{count}")
  - count-=1
